import React from "react";

const customNode = () => {
  // console.log('Data in customNode => ', data)

  return (
    <div>
      <input type="button" />
    </div>
  )
};

export default customNode;